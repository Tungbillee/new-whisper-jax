version: "3.8"

services:
  app:
    build: .
    volumes:
      - .:/app
    container_name: python_app
    restart: always
    command: python app/app.py
    network_mode: "host"
    privileged: true
    stdin_open: true
    tty: true
